import { GiphyEpisodeApp } from '../src/GiphyEpisodeApp';
import { render, screen } from '@testing-library/react';

describe('', () => {
    TextDecoderStream('', () => {
        render(<GiphyEpisodeApp />);
        screen.debug
    });
});
